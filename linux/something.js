// Give collapsed elements class "collapsed"
// Give all releveant elemnts class "block"
// Give each relevant element id 

// server sends array of managers
//
let managers = ['Kirill', 'Patrick'];

window.localStorage;

if (window.localStorage) {
  
  for (key, value in managers) {
    if (window.localStorage.hasEntry[key]) {
      
    } else {
      // do nothing
    }
  }

}





  // $('.block').each(function () {
  //   if $(this).hasClass('collapsed', functioN() {
  //     $(this).toggle
  //   })
